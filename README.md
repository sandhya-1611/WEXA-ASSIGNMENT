Minimal skeleton repo. Will include:
- MERN stack (Node/Express/Mongo + React/Vite)
- Agentic triage (deterministic stub)
- Docker Compose + seed + tests

Work-in-progress. See commit history for steps.